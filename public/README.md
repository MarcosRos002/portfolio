# public/ — static assets

This folder is served at the site root. Phase 0 left it empty except for this note.

**Assets to add later (Phase 1):**

- `cv-es.pdf` and `cv-en.pdf` — copy from `/home/main/Desktop/Portfolio/*.pdf` (the
  Spanish and English CVs). Footer/About will link to these for download.
  _Do not copy them in Phase 0._
- `og-image.png` — Open Graph / social-share image referenced by `app/layout.tsx`
  metadata.
- `favicon.ico` (or `app/icon.png`) — site icon.

Keep filenames stable since they're referenced from code.
